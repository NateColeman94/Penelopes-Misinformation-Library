(function(){"use strict";let index=0,last=-1;const $=id=>document.getElementById(id);function show(next){const a=window.PENELOPE_HALL_EXHIBITS||[];if(!a.length)return;if(typeof next==="number")index=(next+a.length)%a.length;const x=a[index];$("hallImage").src=x.image;$("hallImage").alt=`Museum-style illustration for ${x.title}`;$("hallTitle").textContent=x.title;$("hallMisunderstanding").textContent=`“${x.misunderstanding}”`;$("hallCuratorNote").textContent=x.note;last=index;}function random(){const a=window.PENELOPE_HALL_EXHIBITS||[];if(a.length<2)return show(0);let n=index;while(n===index)n=Math.floor(Math.random()*a.length);show(n);}function init(){const a=window.PENELOPE_HALL_EXHIBITS||[];if(!a.length)return;index=Math.floor(Math.random()*a.length);show(index);$("hallPrevBtn")?.addEventListener("click",()=>show(index-1));$("hallNextBtn")?.addEventListener("click",()=>show(index+1));$("hallAnotherBtn")?.addEventListener("click",random);}window.PenelopeHall={init};document.addEventListener("DOMContentLoaded",init);})();